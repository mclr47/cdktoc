import { ObserveHashItemDirective } from './observe-hash-item.directive';

describe('ObserveHashItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ObserveHashItemDirective();
    expect(directive).toBeTruthy();
  });
});

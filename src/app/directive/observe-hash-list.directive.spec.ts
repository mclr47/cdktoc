import { ObserveHashListDirective } from './observe-hash-list.directive';

describe('ObserveHashListDirective', () => {
  it('should create an instance', () => {
    const directive = new ObserveHashListDirective();
    expect(directive).toBeTruthy();
  });
});
